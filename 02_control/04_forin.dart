main() {
  List<String> heroes = ['Hulk', 'Captain America', 'Spiderman'];
  // Con for
  // for (int i = 0; i < heroes.length; i++) {
  //   print(heroes[i]);
  // }

  // Con for in
  for (String heroe in heroes) {
    print(heroe);
  }
}
